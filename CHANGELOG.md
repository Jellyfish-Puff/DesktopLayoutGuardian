# 更新记录

## 0.6.0（开发中）

- 重新设计概览页，以当前显示方案、主要操作和方案预览为核心。
- 将采集详情从概览移至设置页的高级诊断区域，减少首页技术信息干扰。
- 中文字体优先使用 Microsoft YaHei UI，并启用 ClearType、固定字形提示和像素对齐。
- 重做按钮、导航、复选框、自动恢复开关、输入框和卡片视觉样式。
- 增加时段问候、概览自动恢复开关和横向已保存方案预览。
- 统一方案、恢复记录和设置页面的标题、留白、圆角与控件尺寸。
- 新增专属应用图标，并统一应用到程序文件、窗口、侧栏品牌区和系统托盘。

## 0.5.0

- 新增独立设置页面，将开机启动、成功通知和后台状态集中管理。
- 新增自动恢复总开关；关闭后仍识别显示环境，并保留手动保存、恢复和撤销。
- 新增快速、标准、稳健三档检测策略，稳健模式适合 UU 超级屏或较慢的显示切换。
- 支持设置每个方案保留 5、10、20 或 50 份历史版本。
- 新增旧历史与无引用预览清理，并保留当前、历史和安全删除方案仍引用的预览。
- 新增安全清空全部当前方案，原始记录统一移入 `deleted` 备份目录。
- 新增诊断日志清理、数据目录入口和 GitHub 项目入口。

## 0.4.0

- 保存布局时自动生成纯桌面预览，由壁纸、桌面图标及坐标合成，不会截取已打开窗口。
- 启用“显示方案”页面，以卡片形式查看预览、显示环境和图标数量。
- 支持重命名显示方案及可恢复删除；删除不会立即永久清除数据。
- 启用“恢复记录”页面，可查看覆盖保存前的旧版本并临时恢复。
- 兼容方案存在多个候选时，固定选择最近更新的版本。
- 旧版本保存的方案继续兼容；再次保存后会自动补充预览。

## 0.3.0

- 增加单实例保护，重复启动时唤醒现有窗口。
- 完善系统托盘菜单，支持保存、恢复、撤销、重新检测和退出。
- 增加可选的 Windows 登录后静默启动。
- 显示变化防抖后执行双次配置采集，确认分辨率与缩放稳定再恢复。
- 保存和恢复桌面布局时使用互斥保护，并在 Explorer 暂时不可用时重试。
- 每次恢复前持久化当前布局，可在相同显示环境中撤销最近一次恢复。
- 设置、方案和恢复快照改用原子文件写入。
- 诊断日志达到 2 MB 后保留上一份并轮换。
- 增加恢复成功通知开关；失败通知始终显示。
- 增加不依赖第三方测试框架的核心回归测试。

## 0.2.0

- 首个完成实机验证的可用版本。
- 支持 Windows 11 单显示器环境识别。
- 支持按显示器、分辨率、缩放和方向保存及自动恢复桌面图标布局。
- 已验证笔记本内屏、ROG XG27AQDMES 和 UU 远程超级屏。
